CREATE TABLE movieTitleGenerator (
    id INTEGER PRIMARY KEY, 
    adj VARCHAR(100), 
    noun VARCHAR(100)
);

CREATE TABLE movies (
	id INTEGER PRIMARY KEY, 
	name VARCHAR(100), 
	discription VARCHAR(200)
);
